services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: issa_wholesale
    ports:
      - "5432:5432"
    volumes:
      - issa_pg_data:/var/lib/postgresql/data

volumes:
  issa_pg_data:

